Graph Mind is a web-based application that converts text documents and web pages into an interactive knowledge graph. It extracts key concepts as nodes and their relationships as edges, storing them in a Neo4j database. Users can upload files or URLs, and each document appears as a large parent node that can expand into connected sub-nodes. Every node shows a number badge with its potential connections, and edges are drawn with clear, visible lines.